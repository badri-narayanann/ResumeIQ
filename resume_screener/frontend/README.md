# ResumeIQ Frontend

This folder contains the React frontend for ResumeIQ.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## API

The frontend communicates with the backend via the `/api` prefix.
Use `VITE_API_BASE` to override the backend host when needed.
