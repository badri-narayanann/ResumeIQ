# Testing

The repository uses `pytest` for automated backend tests.

## Run tests

```powershell
cd resume_screener
python -m pytest -q
```

## What is covered

- API status and health checks
- resume screening workflow
- validation of required fields
- backend configuration and database initialization
